feat(pdf): implement lazy loading with prefetch and GoodNotes-style navigation

Major features:
- PDF lazy loading: Save PDF to disk, render pages on-demand with LRU cache (50MB/20 pages)
- Initial prefetch: First 3 pages loaded before editor opens, pages 4-10 in background
- Dynamic prefetch: Auto-load pages around current page (±3) when navigating
- GoodNotes-style sidebar: Compact page navigator (140px) with thumbnails showing PDF content
- Multi-page drawing fix: Strokes now correctly bound to their respective pages
- Canvas improvements: Warm off-white (0xFFF5F5F0) surrounding area for notebook/PDF modes
- Layout fix: Sidebar only compresses canvas, not toolbar
- UI polish: Document title overflow handling, PDF initial positioning fixes

Technical implementation:
- Added pdfPageRenderProvider (FutureProvider.family) for lazy rendering
- Added pdfPrefetchManagerProvider for intelligent page prefetching
- PageBackground now stores pdfFilePath instead of full pdfData
- Sidebar repositioned under toolbar using Column+Row layout
- Canvas transform initialization improved to handle page/document changes
- PDF thumbnails rendered via pdfPageRenderProvider in PageThumbnail widget

Performance:
- 144-page PDF import: ~5-10 seconds (metadata only, no rendering)
- Per-page render: ~1 second on first view, instant from cache
- Memory-efficient: LRU cache with 50MB/20-page limits
